server:
  port: 8282
  mode: debug

database:
  host: localhost
  port: "5432"
  user: postgres
  password: postgres
  name: notification_service
  ssl_mode: disable

resend:
  api_key: "your-resend-api-key"
  from_email: "notifications@yourcompany.com"

redis:
  host: localhost
  port: 6379
  password: ""
  db: 0

metrics:
  enabled: true
  port: 9090 